name: recommender
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - sqlalchemy>=2.0.0
    - pandas>=2.0.0
    - numpy>=1.24.0
    - scikit-learn>=1.3.0
    - scipy>=1.10.0
    - pytest>=7.4.0
    - pytest-cov>=4.1.0
    # PyTorch with CUDA: Install with custom index after environment creation
    # Run: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
    # For CPU-only: pip install torch torchvision torchaudio
    - torch>=2.0.0
    - torchvision>=0.15.0
    - torchaudio>=2.0.0
    - torch-geometric>=2.3.0
