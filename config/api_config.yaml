# API configuration
api:
  title: GraphSAGE Recommender API
  version: "1.0.0"
  host: "0.0.0.0"
  port: 8000

cors:
  allow_origins: ["*"]
  allow_credentials: true
  allow_methods: ["*"]
  allow_headers: ["*"]

logging:
  level: INFO
